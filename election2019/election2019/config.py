from pathlib import Path

data_dir = Path('../data/election_data')
shp_dir = Path('../data/shapefiles')
census_dir = Path('../data/census')

contests_path = data_dir / 'contests'
results_path = data_dir / 'results'
